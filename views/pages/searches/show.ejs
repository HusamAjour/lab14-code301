<%- include('../../layout/head') %>
<%- include('../../layout/header') %>
<main>
    <section class="books-cards">
        <% books.forEach((b, i) =>{   %>
        <div class="book-card">
            <img src='<%= b.image_url %>' alt="<%= b.title %>">
            <h1><%= b.title %> </h1>
            <h2><%= b.author %></h2>
            <% let x = b.description.split(' ').slice(0,30); %>
            <p><%= x.join(' '); %>...</p>
            <form action="/books" method="post">
                <input type='hidden' name="title" value="<%= b.title %>">
                <input type='hidden' name="author" value="<%= b.author %>">
                <input type='hidden' name="isbn" value="<%= b.isbn  %>">
                <input type='hidden' name="image_url" value="<%= b.image_url %>">
                <input type='hidden' name="description" value="<%= b.description %>">

                <div class="submit-btn">
                    <button class="btn" type="submit" >Select this Book</button>
                </div>
            </form>
        </div>
        <% }); %>
    </section>
</main>
<%- include('../../layout/footer') %>

</html>